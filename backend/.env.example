PORT=4000
ML_SERVICE_URL=http://localhost:8000
CLIENT_ORIGIN=http://localhost:5173
# Optional: override where the SQLite file is written (defaults to backend/screening.db)
# DATABASE_PATH=./screening.db
